# Copy this file to a new file named ".env" and paste your real key after the "=".
# Do NOT use quotes. Do NOT add spaces around the "=".
# Your real .env file is ignored by git (see .gitignore), so your key stays private.

ANTHROPIC_API_KEY=your_key_here
